# API / Backend
FILE_SERVER_URL=http://localhost:8080

# SSH Configuration for both App (listing files) and Proxy (mounting files)
SSH_USER=user
SSH_HOST=100.76.133.15
# This is the path INSIDE the container where the key will be.
SSH_PRIVATE_KEY_PATH=/app/ssh/id_ed25519
# This is the remote directory to mount.
REMOTE_DIR="C:/htdocs/VLF"

# Stations config
STATIONS_CONFIG=/app/ssh/stations.yml